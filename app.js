// <!-- NOTE: I can't use the external links for changing the background UI -->
    // <script>
        // $( function() {
        //     $( "div ul li" ).draggable();
        // } );

        $( function() {
            $( ".tasks" ).sortable();
        } );
    // </script>